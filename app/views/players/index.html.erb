<h1>Lista de Jugadores</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Dinero</th>
    </tr>
  </thead>
  <tbody>
    <% @players.each do |player| %>
      <tr>
        <td><%= player.name %></td>
        <td><%= number_to_currency(player.money, unit: "$", precision: 0) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "Agregar nuevo jugador", new_player_path %>
